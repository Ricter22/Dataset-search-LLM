# Experimental Metadata for TARGET and NTCIR-15

| Field | TARGET value | NTCIR-15 value | Evidence type | Source path | Notes |
| --- | --- | --- | --- | --- | --- |
| LLM name/version for pseudoquery generation | gpt-5-nano-2025-08-07 | gpt-5-mini | TARGET: code-backed; NTCIR-15: artifact-backed | TARGET: `legacy/target/TARGET_test.ipynb`<br>NTCIR-15: `experiments/ntcir15/offline_phase/artifacts/run_summary.json` | TARGET recovered runs replay prebuilt artifacts; the notebook model variable is the only checked-in model binding for pseudoquery generation. |
| LLM for query optimization | gpt-5-nano-2025-08-07 | gpt-5-mini | TARGET: code-backed; NTCIR-15: artifact-backed | TARGET: `legacy/target/TARGET_test.ipynb`, `experiments/target/preprocessing_utils.py`<br>NTCIR-15: `experiments/ntcir15/online_phase/artifacts_live/online_run_summary.json`, `experiments/ntcir15/online_phase/run_online_phase.py` | TARGET query optimization is implemented as background generation plus subquery decomposition in `get_subqueries_from_query`. |
| LLM for reranking | Not used / no reranking stage in recovered TARGET runs | gpt-5-mini | TARGET: artifact-backed; NTCIR-15: artifact-backed | TARGET: `experiments/target/rerun_target_recovery.py`, `experiments/target/target_recovery_results_full/metrics_summary.csv`<br>NTCIR-15: `experiments/ntcir15/online_phase/run_online_phase.py`, `experiments/ntcir15/online_phase/prompts.py`, `experiments/ntcir15/online_phase/artifacts_live/online_run_summary.json` | NTCIR-15 reranking uses the same live `llm_model` as query optimization in the checked-in live runs. |
| Embedding model name/version | text-embedding-3-small | text-embedding-3-small | TARGET: code-backed; NTCIR-15: code-backed | TARGET: `experiments/target/preprocessing_utils.py`, `experiments/target/rerun_target_recovery.py`<br>NTCIR-15: `experiments/ntcir15/offline_phase/db_population_evaluation_pack.py`, `experiments/ntcir15/online_phase/run_online_phase.py` | No more specific model revision is persisted in the repo. |
| Vector store / nearest-neighbor backend | Chroma PersistentClient with HNSW vector index | Chroma PersistentClient with HNSW vector index | TARGET: code-backed; NTCIR-15: artifact-backed | TARGET: `legacy/target/TARGET_test.ipynb`, `experiments/target/chroma/chroma.sqlite3`<br>NTCIR-15: `experiments/ntcir15/online_phase/artifacts_live/online_run_summary.json`, `experiments/ntcir15/offline_phase/chroma_ntcir15_eval/chroma.sqlite3` | TARGET recovery code points at `/chroma_db`; the checked-in target-adjacent Chroma SQLite also stores HNSW config. |
| temperature | Not explicitly set in chat completions; SDK/provider default | Not explicitly set in chat completions; SDK/provider default | TARGET: code-backed; NTCIR-15: code-backed | TARGET: `experiments/target/preprocessing_utils.py`<br>NTCIR-15: `experiments/ntcir15/offline_phase/db_population_evaluation_pack.py`, `experiments/ntcir15/online_phase/run_online_phase.py` | Do not interpret unset parameters as a known numeric value. |
| max tokens | Not explicitly set in actual generation calls; SDK/provider default | Not explicitly set in actual generation calls; SDK/provider default | TARGET: code-backed; NTCIR-15: code-backed | TARGET: `experiments/target/preprocessing_utils.py`<br>NTCIR-15: `experiments/ntcir15/offline_phase/db_population_evaluation_pack.py`, `experiments/ntcir15/online_phase/run_online_phase.py` | NTCIR-15 only sets `max_tokens=16` for API preflight, not for the real experiment calls. |
| Whether the same prompt template is used across datasets | Yes | Yes | TARGET: code-backed; NTCIR-15: code-backed | TARGET: `experiments/target/preprocessing_utils.py`<br>NTCIR-15: `experiments/ntcir15/offline_phase/db_population_evaluation_pack.py`, `experiments/ntcir15/online_phase/prompts.py` | Both tracks reuse shared prompt-builder functions rather than per-dataset templates. |
| Whether prompts are in appendix / supplementary material | Unavailable from repo; requires manuscript source | Unavailable from repo; requires manuscript source | TARGET: unavailable; NTCIR-15: unavailable | Manuscript source not included in this repository | Repo only contains implementation prompts, not paper appendix metadata. |
| Number of pseudoqueries per dataset | 10 per dataset | Variable-length. With semantic (full-system collection): 31.97 avg, 20-56 range (3421 total across 107 datasets). | TARGET: code-backed; NTCIR-15: artifact-backed | TARGET: `experiments/target/preprocessing_utils.py`<br>NTCIR-15: `experiments/ntcir15/offline_phase/artifacts/dataset_info_evaluation.jsonl`, `experiments/ntcir15/offline_phase/artifacts/run_summary.json` | NTCIR-15 prompt says 'Generate as many queries as required', so the count is not fixed. |
| Top-K retrieved pseudoqueries per subquery | 100 | 5 | TARGET: code-backed; NTCIR-15: artifact-backed | TARGET: `experiments/target/preprocessing_utils.py`<br>NTCIR-15: `experiments/ntcir15/online_phase/artifacts_live/online_run_summary.json`, `experiments/ntcir15/online_phase/run_online_phase.py` | TARGET retrieves 100 Chroma hits per subquery before frequency aggregation; NTCIR-15 retrieves 5 hits per subquery before deduplication and reranking. |
| Max number of subqueries N, if capped | 10 subqueries, explicitly capped by prompt | No explicit cap in code. Observed in live `full` runs: 16-35 subqueries/query (20.5 avg original, 23.9 avg complex). | TARGET: code-backed; NTCIR-15: code-backed + artifact-backed | TARGET: `experiments/target/preprocessing_utils.py`<br>NTCIR-15: `experiments/ntcir15/online_phase/prompts.py`, `experiments/ntcir15/online_phase/run_online_phase.py`, `experiments/ntcir15/online_phase/artifacts_live/online_run_summary.json` | NTCIR-15 live counts come from `artifacts_live/runs/*/query_results/*.json`. |
| Candidate set size before reranking | N/A; no reranking stage | 23.4 avg unique candidates/query in live `full` runs (21.8 original, 25.0 complex; 9-34 range). | TARGET: artifact-backed; NTCIR-15: artifact-backed | TARGET: `experiments/target/target_recovery_results_full/metrics_summary.csv`<br>NTCIR-15: `experiments/ntcir15/online_phase/artifacts_live/online_run_summary.json`, `experiments/ntcir15/online_phase/artifacts_live/runs/*/query_results/*.json` | NTCIR-15 candidate-set size is the deduplicated set that is passed into the reranker. |
| Similarity metric | L2 | L2 | TARGET: artifact-backed; NTCIR-15: artifact-backed | TARGET: `experiments/target/chroma/chroma.sqlite3`<br>NTCIR-15: `experiments/ntcir15/offline_phase/chroma_ntcir15_eval/chroma.sqlite3` | Chroma SQLite collection config stores `space="l2"` for the HNSW vector index. |
| How tables are cleaned | First row is promoted to header; on multi-line-header failure, header rows are merged, apostrophes are removed, whitespace is normalized to `_`, names are lowercased, and duplicates are disambiguated. | Files are robustly parsed from CSV/TSV/TXT/XLS/XLSX/JSON/HTML/ZIP/GZ; bad CSV lines are skipped; large streamable files use hybrid sampled profiling; complex values are sanitized to JSON/text for profiling. | TARGET: code-backed; NTCIR-15: code-backed | TARGET: `experiments/target/preprocessing_utils.py`<br>NTCIR-15: `experiments/ntcir15/offline_phase/db_population_evaluation_pack.py` | Neither track performs aggressive cell-level normalization beyond what is described here. |
| Type detection rules | Uses `datamart_profiler` structural types, then `pandas.is_numeric_dtype` and `pandas.is_datetime64_any_dtype`; all other columns are treated as categorical/text-like. | Uses the same `datamart_profiler` structural type summary and pandas numeric/datetime checks; global scans also accumulate numeric counts and per-column null/non-null stats. | TARGET: code-backed; NTCIR-15: code-backed | TARGET: `experiments/target/preprocessing_utils.py`<br>NTCIR-15: `experiments/ntcir15/offline_phase/db_population_evaluation_pack.py` | Coverage/range summaries are emitted when profiler metadata includes them. |
| Handling missing values | No explicit blanket imputation in wrapper code. Categorical top-value summaries use `dropna()`, so missing entries are excluded from frequency counts; other handling is delegated to parsers/profiler. | No blanket imputation in wrapper code. Global scan reports missing counts and percentages per column; categorical frequency summaries use `dropna()` and value counts without nulls. | TARGET: code-backed; NTCIR-15: code-backed | TARGET: `experiments/target/preprocessing_utils.py`<br>NTCIR-15: `experiments/ntcir15/offline_phase/db_population_evaluation_pack.py` | Notebook outputs also show `datamart_profiler` internals warning about internal `fillna('')`, but that is not an explicit wrapper rule. |
| Offline indexing cost | Unavailable from checked-in TARGET artifacts | 3563.60s total for the checked-in index stage (111 discovered, 108 processable datasets). | TARGET: unavailable; NTCIR-15: artifact-backed | TARGET: `legacy/target/TARGET_test.ipynb`<br>NTCIR-15: `experiments/ntcir15/offline_phase/artifacts/run_summary.json` | TARGET notebook contains commented timing prints, but no persisted offline indexing runtime. |
| Average online latency | Recovery replay only, not original end-to-end LLM latency. `queryopt_sem` k=5 totals: OTT-QA 0.02491s/run, FetaQA 0.01169s/run; per-query averages round to about 0.00001s/query in the persisted metrics. | Live `full` runs: original 168.11s/query (run-total cross-check 168.15s), complex 201.21s/query (run-total cross-check 201.24s). | TARGET: artifact-backed; NTCIR-15: artifact-backed | TARGET: `experiments/target/target_recovery_results_full/metrics_summary.csv`<br>NTCIR-15: `experiments/ntcir15/online_phase/artifacts_live/online_run_summary.json`, `experiments/ntcir15/online_phase/artifacts_live/runs/*/query_results/*.json` | The checked-in `experiments/ntcir15/online_phase/artifacts/online_run_summary.json` is dry-run and is intentionally excluded. |
| Approximate token usage | Not persisted. Rough proxy only: offline indexing uses about 2 LLM calls per dataset (semantic profile + pseudoqueries), and online query optimization uses about 2 LLM calls per user query (background + subquery decomposition). | Not persisted. Rough proxy only: offline indexing uses about 2 LLM calls per processable dataset (semantic + instructions); live `full` retrieval uses about 3 LLM calls per query (background + subquery decomposition + rerank), plus 1 extra call/query when complex queries are generated. | TARGET: code-inferred; NTCIR-15: code-inferred | TARGET: `experiments/target/preprocessing_utils.py`, `legacy/target/TARGET_test.ipynb`<br>NTCIR-15: `experiments/ntcir15/offline_phase/db_population_evaluation_pack.py`, `experiments/ntcir15/online_phase/run_online_phase.py` | These are call-count proxies, not actual prompt/completion token totals. |

