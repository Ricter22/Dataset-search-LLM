# Evaluation-Ready Pack

"
"This package is aligned to successfully downloaded datasets only (`status=ok`).

"
"## Files
"
"- `topics.tsv`: selected queries (10).
"
"- `qrels.txt`: qrels filtered to selected queries and downloaded datasets only.
"
"- `dataset_ids.txt`: downloaded dataset IDs used in evaluation.
"
"- `download_manifest_ok.csv`: manifest rows with `status=ok` only.
"
"- `datasets/`: downloaded dataset files (domain/dataset_id structure).
"
"- `pack_report.json`: summary stats, including per-query relevance counts.
"
